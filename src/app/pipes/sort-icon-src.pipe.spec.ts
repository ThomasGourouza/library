import { SortIconSrcPipe } from './sort-icon-src.pipe';

describe('SortIconSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new SortIconSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
