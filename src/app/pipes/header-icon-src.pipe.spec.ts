import { HeaderIconSrcPipe } from './header-icon-src.pipe';

describe('HeaderIconSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderIconSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
