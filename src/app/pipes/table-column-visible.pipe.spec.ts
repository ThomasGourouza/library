import { TableColumnVisiblePipe } from './table-column-visible.pipe';

describe('TableColumnVisiblePipe', () => {
  it('create an instance', () => {
    const pipe = new TableColumnVisiblePipe();
    expect(pipe).toBeTruthy();
  });
});
