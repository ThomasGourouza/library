import { SelectListPipe } from './select-list.pipe';

describe('SelectListPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectListPipe();
    expect(pipe).toBeTruthy();
  });
});
