export enum Language {
    UNKNOWN,
    FR,
    EN,
    ES,
    DE,
    IT,
    JA,
    ZH,
    RU,
    DA,
}

export enum Type {
    FICTION,
    NON_FICTION,
    ANTHOLOGY,
}

export enum Category {
    MYSTERY,
    SCIFI,
    BIOGRAPHY,
}

export enum Audience {
    ADULT,
    TEEN,
    CHILD,
}

export enum Status {
    AVAILABLE,
    CHECKED_OUT,
    RESERVED,
}

export enum Country {
    FRANCE,
    GERMANY,
    SPAIN,
}
